services:
  - type: web
    name: tic-tac-toe-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
        